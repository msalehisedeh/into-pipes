import { ModuleWithProviders } from '@angular/core';
import { ComponentPool } from '../common/component.pool';
export declare class ShareIntoPipeModule {
    static forRoot(): ModuleWithProviders;
    constructor(pool: ComponentPool);
}
